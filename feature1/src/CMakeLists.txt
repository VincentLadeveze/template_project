target_sources(${PROJECT_NAME} PRIVATE
                ${CMAKE_CURRENT_LIST_DIR}/feature1.cpp
                )

target_include_directories(${PROJECT_NAME} PUBLIC 
                            "${CMAKE_CURRENT_LIST_DIR}/../include"
                            )
